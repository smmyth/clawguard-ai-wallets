{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:mantle": "hardhat run scripts/deploy.ts --network mantleSepolia",
    "deploy:v2:mantle": "hardhat run scripts/deploy-v2.ts --network mantleSepolia",
    "verify:mantle": "hardhat run scripts/verify.ts --network mantleSepolia"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
